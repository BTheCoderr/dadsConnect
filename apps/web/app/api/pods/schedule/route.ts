import { jsonOk } from "@/lib/http"

export async function GET() {
  return jsonOk({ items: [] })
}

import { jsonOk } from "@/lib/http"

// Placeholder: Pod sessions are not in schema; return an empty list for now
export async function GET() {
  return jsonOk({ items: [] })
}


